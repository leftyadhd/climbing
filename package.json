{
  "name": "climbing-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && pagefind --site dist && netlify deploy --prod --dir=dist"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0",
    "astro-pagefind": "^1.8.0",
    "pagefind": "^1.1.1",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  }
}
